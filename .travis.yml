language: node_js
node_js:
      - 8

cache:
      directories:
            - node_modules

install:
      - 'yarn add coveralls'

script:
      - yarn test --coverage --coverageReporters=text-lcov --detectOpenHandles --forceExit | coveralls
      - yarn build
